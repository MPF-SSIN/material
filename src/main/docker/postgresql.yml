version: '2'
services:
  ssin-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/ssin/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=ssin
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
